name: Build & Deploy

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      service:
        description: '재배포할 서비스'
        required: true
        default: all
        type: choice
        options: [ all, backend, payment-mock ]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  changes:
    runs-on: ubuntu-24.04
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      payment-mock: ${{ steps.filter.outputs.payment-mock }}
      deploy-files: ${{ steps.filter.outputs.deploy-files }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect changed paths
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            backend:
              - 'src/**'
              - 'build.gradle'
              - 'settings.gradle'
              - 'Dockerfile'
              - 'gradle/**'
            payment-mock:
              - 'payment-mock/**'
            deploy-files:
              - 'docker/**'
              - 'observability/**'

  build-backend:
    needs: changes
    if: needs.changes.outputs.backend == 'true' || contains(fromJSON('["all","backend"]'), github.event.inputs.service)
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: gradle

      - name: Build JAR (verify)
        run: ./gradlew clean build -x test --no-daemon --build-cache

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push Image
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: |
            ${{ vars.DOCKER_NAMESPACE || secrets.DOCKER_USERNAME }}/givemeticket:backend
            ${{ vars.DOCKER_NAMESPACE || secrets.DOCKER_USERNAME }}/givemeticket:backend-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-payment-mock:
    needs: changes
    if: needs.changes.outputs.payment-mock == 'true' || contains(fromJSON('["all","payment-mock"]'), github.event.inputs.service)
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push Image
        uses: docker/build-push-action@v5
        with:
          context: ./payment-mock
          platforms: linux/amd64
          push: true
          tags: |
            ${{ vars.DOCKER_NAMESPACE || secrets.DOCKER_USERNAME }}/givemeticket:payment-mock
            ${{ vars.DOCKER_NAMESPACE || secrets.DOCKER_USERNAME }}/givemeticket:payment-mock-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: [ changes, build-backend, build-payment-mock ]
    if: |
      always() &&
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled') &&
      (needs.changes.outputs.backend == 'true' ||
       needs.changes.outputs.payment-mock == 'true' ||
       needs.changes.outputs.deploy-files == 'true' ||
       github.event_name == 'workflow_dispatch')
    runs-on: [ self-hosted, prod ]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fetch config submodule (private)
        env:
          SUBMODULE_KEY: ${{ secrets.SUBMODULE_KEY }}
        run: |
          set -euo pipefail
          REPO_URL="https://github.com/givemeticket/givemeticket-submodule"
          git -c url."https://x-access-token:${SUBMODULE_KEY}@${REPO_URL#https://}".insteadOf="${REPO_URL}" \
            submodule update --init --remote docker/config
          test -f docker/config/application-prod.yml
          test -f docker/config/mysql.env
          test -f docker/config/grafana.env

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Sync deploy files
        env:
          DEPLOY_DIR: ${{ vars.DEPLOY_DIR }}
          DOCKER_NAMESPACE: ${{ vars.DOCKER_NAMESPACE || secrets.DOCKER_USERNAME }}
          DOMAIN: ${{ vars.DOMAIN || 'givemeticket.site' }}
        run: |
          set -euo pipefail
          DEPLOY_DIR="${DEPLOY_DIR:-$HOME/givemeticket-prod}"
          echo "📁 배포 디렉터리: $DEPLOY_DIR"
          mkdir -p "$DEPLOY_DIR/config"

          {
            printf 'DOCKER_NAMESPACE=%s\n' "$DOCKER_NAMESPACE"
            printf 'DOMAIN=%s\n' "$DOMAIN"
          } > "$DEPLOY_DIR/.env"
          chmod 600 "$DEPLOY_DIR/.env"

          cp docker/docker-compose.prod.yml "$DEPLOY_DIR/docker-compose.yml"
          cp docker/deploy.sh               "$DEPLOY_DIR/deploy.sh"
          cp docker/config/application-prod.yml "$DEPLOY_DIR/config/application-prod.yml"
          cp docker/config/mysql.env            "$DEPLOY_DIR/config/mysql.env"
          cp docker/config/grafana.env          "$DEPLOY_DIR/config/grafana.env"
          rm -rf "$DEPLOY_DIR/observability" "$DEPLOY_DIR/nginx"
          cp -R observability "$DEPLOY_DIR/observability"
          cp -R nginx "$DEPLOY_DIR/nginx"
          mkdir -p "$DEPLOY_DIR/certbot/conf" "$DEPLOY_DIR/certbot/www"
          chmod +x "$DEPLOY_DIR/deploy.sh"

      - name: Deploy payment-mock
        if: needs.build-payment-mock.result == 'success'
        env:
          DEPLOY_DIR: ${{ vars.DEPLOY_DIR }}
        run: |
          DEPLOY_DIR="${DEPLOY_DIR:-$HOME/givemeticket-prod}"
          "$DEPLOY_DIR/deploy.sh" payment-mock

      - name: Deploy backend
        if: needs.build-backend.result == 'success' || needs.changes.outputs.deploy-files == 'true'
        env:
          DEPLOY_DIR: ${{ vars.DEPLOY_DIR }}
        run: |
          DEPLOY_DIR="${DEPLOY_DIR:-$HOME/givemeticket-prod}"
          "$DEPLOY_DIR/deploy.sh" backend
